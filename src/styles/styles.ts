import { unsafeCSS } from "lit";
import _style from "./styles.css?inline";
import _colors from "./colors.css?inline";

export const style = unsafeCSS(_style);
export const colors = unsafeCSS(_colors);

